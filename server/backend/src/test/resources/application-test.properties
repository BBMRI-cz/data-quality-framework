spring.datasource.url=jdbc:tc:postgresql:17:///test
spring.datasource.driver-class-name=org.testcontainers.jdbc.ContainerDatabaseDriver

# Enable Flyway clean command for tests (allows database cleanup between test classes)
spring.flyway.clean-disabled=false
